boostrap字体组件尝试：
    1.安装bootstrap:npm install bootstrap@3
    2.main.js里面引用bootstrap
    3.在webpack.config.js中配置字体

providePlugin使用：
    1.自动加载模块，不需要自己import，能够解决复杂的依赖关系
    2.插件webpack自带，不需要装，直接在webpack.config.js进行配置